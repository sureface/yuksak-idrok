import React from 'react';
import MainHeader from "../components/mainHeader";

const Home = () => {
    return (
        <div className="app">
            <MainHeader />
        </div>
    );
};

export default Home;